{
  "name": "job-retry",
  "version": "1.0.0",
  "description": "",
  "main": "./app/index.js",
  "scripts": {
    "lint": "eslint . --ext .ts",
    "build": "rm -rf ./build && tsc",
    "test": "jest --watchAll --no-cache --coverage",
    "prettier": "prettier --config .prettierrc 'app/**/*.ts' --write"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-prettier": "^4.2.1",
    "jest": "^29.5.0",
    "prettier": "^2.8.8",
    "ts-jest": "^29.1.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.1.3"
  }
}
